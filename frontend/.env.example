# RuView Connection Settings
# Replace localhost with your backend server's IP address
NEXT_PUBLIC_WS_URL=ws://YOUR_SERVER_IP:8000/ws/sensing
NEXT_PUBLIC_API_URL=http://YOUR_SERVER_IP:8000

# Development Tools
# Set to 'true' to use the local generator instead of a backend connection
NEXT_PUBLIC_USE_MOCK=false
